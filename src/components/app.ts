export class App {
    constructor() {
        // const model = new Model();
        // const view = new View();
        // this.controller = new Controller(model, view);
    }

    start(): void {
        // this.controller.start();
    }
}
